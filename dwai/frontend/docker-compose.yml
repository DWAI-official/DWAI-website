services:
  db:
    image: postgres:15-alpine
    restart: always
    environment:
      - POSTGRES_USER=dwai
      - POSTGRES_PASSWORD=dwai$5528
      - POSTGRES_DB=donation
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: